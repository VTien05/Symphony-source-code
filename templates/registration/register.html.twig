{% extends 'products/index.html.twig' %}

{% block header %}
{% endblock %}


{% block container %}
    

    {{ form_start(registrationForm) }}
        {# {{ form_row(registrationForm.email) }} #}
        {# {{ form_row(registrationForm.plainPassword, {
            label: 'Password'
        }) }} #}

        {# {{ form_widget(registrationForm.email, {'attr':{'placehozlder':'Enter Email'}}) }} #}
        {# {{ form_widget(registrationForm.plainPassword, {'attr':{'placeholder':'Enter Password'}}) }} #}
        {# {{ form_row(registrationForm.agreeTerms) }} #}
        {# <button type="submit" class="btn">Register</button> #}
    

    <div id="sign-up">
        <div class="signup">
            <h1>Sign Up</h1>
            <p>Please fill in this form to create an account.</p>
            <hr>  
            <label for="email"><b>Email</b></label>      
            {{ form_widget(registrationForm.email, {'attr':{'style':'padding:8px 4px; margin-bottom:10px; margin-top: 10px;','placeholder':'Enter Email'}}) }}
    
            <label for="psw"><b>Password</b></label>
            {{ form_widget(registrationForm.plainPassword, {'attr':{'style':'padding:8px 4px; margin-bottom:15px; margin-top: 10px;','placeholder':'Enter Password'}}) }}
       
            {{ form_widget(registrationForm.agreeTerms, {'label':'By creating an account you agree to our Terms and Privacy.'}) }}
        
            {{ form_label(registrationForm.agreeTerms, 'By creating an account you agree to our Terms and Privacy') }}
            
            <div class="clearfix">
                <button type="button" class="cancelbtn btn" onclick="location.href = '{{path('app_products')}}';">Cancel</button>
                <button type="submit" class="signupbtn btn">Sign Up</button>
            </div>
        </div>
    </div>
    {{ form_end(registrationForm) }}
{% endblock %}

{% block footer %}
{% endblock %}
