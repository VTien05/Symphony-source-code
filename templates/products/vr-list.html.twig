{% extends 'products/index.html.twig' %}

{% block container %}
    {% for a in vr %}    
        <div class="vr-list">
            <div class="box">
                <div class="product__img">
                    <img width="557" height="374" src="{{a.img}}" alt="">
                </div>
        
                <div class="product__video">
                    
                </div>
        
                <div class="product__disc">
                    <div class="product__disc--content">
                        <div class="disc__content--about">
                            <h1>{{a.name}}</h1>
                            <span>get ready to step into a new world</span>
                            <p>{{a.description}}</p>
                        </div>
        
                        <div class="product__view--more">
                            <p>view all specifications</p>
                            <div class="view__more--block"></div>
                        </div>
                    </div>
                </div>
        
                <div class="product_buttons">
                    <button class="btn wishlist">${{a.price}}</button>
                    <button class="btn buy">Out of Stock</button>
                </div>
            </div>
        </div>
    {% endfor %}
{% endblock %}

